[pytest]
testpaths = tests
pythonpath = src .
addopts = -ra --strict-markers
log_level = INFO
markers =
    smoke: fast, high-value checks that gate everything else
    negative: invalid-input and unauthorized-access cases
base_url = https://restful-booker.herokuapp.com
